To use, install GNU Stow and, while in this directory, run `stow -Rv PACKAGE` to
symlink its files to your home directory. For example,

    stow -Rv vim

Will create symlinks `~/.vimrc`, `~/.gvimrc`, etc.
